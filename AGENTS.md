# Guidelines

Never remove existing comments in the codebase unless they are clearly outdated or incorrect.

Always run `pnpm lint` at the root of the repository to check for linting errors. Fix all linting errors and iterate until there are none left before finishing.
