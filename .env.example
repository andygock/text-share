# Example .env file for text-share
# Set global and per-IP upload rate limits (per hour)
GLOBAL_UPLOAD_LIMIT=100
PER_IP_UPLOAD_LIMIT=10

# 10MB = 10 * 1024 * 1024 bytes
# Set the maximum size for image uploads
MAX_IMAGE_UPLOAD_SIZE_BYTES=10485760

# Text update allowances per window (shared by clients behind the same IP)
GLOBAL_TEXT_LIMIT=600
PER_IP_TEXT_LIMIT=60
TEXT_LIMIT_WINDOW_SECONDS=10

# Specify allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://ts.gock.net

# Keep false for HTTP/LAN deployments. Set true only when the public app is HTTPS.
ENABLE_UPGRADE_INSECURE_REQUESTS=false

# Trust X-Forwarded-For only from these reverse proxy IPs (comma-separated)
TRUSTED_PROXY_IPS=127.0.0.1,::1

# Maximum UTF-8 bytes accepted per WebSocket text update
MAX_TEXT_BYTES=65536

# All numeric settings must be positive integers.
INVITE_TTL_MS=30000
INVITE_MAX_ATTEMPTS=5
UPLOAD_TIMEOUT_MS=60000
MAX_IMAGE_PROCESSING=2
MAX_IMAGE_PIXELS=40000000
MAX_BUFFERED_BYTES=2097152
MAX_CHUNKS=4096
MAX_CHUNK_BYTES=131072
